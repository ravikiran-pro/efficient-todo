{
  "name": "efficient-todo",
  "displayName": "Efficient Todo",
  "repository": {
    "type": "git",
    "url": "https://github.com/ravikiran-pro/efficient-todo.git"
  },
  "publisher": "efficient-todo",
  "description": "Track todo of your coding activities with pomodoro and timer",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.56.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:efficient-todo.showTaskManager",
    "onCommand:efficient-todo.addTask",
    "onCommand:efficient-todo.startTask",
    "onCommand:efficient-todo.stopTask",
    "onView:taskManager"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "taskManager",
          "title": "Task Manager",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "explorer": [
        {
          "id": "taskManager",
          "name": "Task Manager"
        }
      ],
      "taskManager": [
        {
          "id": "taskList",
          "name": "Tasks"
        }
      ]
    },
    "commands": [
      {
        "command": "efficient-todo.showTaskManager",
        "title": "Show Task Manager"
      },
      {
        "command": "efficient-todo.addTask",
        "title": "Add Task"
      },
      {
        "command": "efficient-todo.startTask",
        "title": "Start Task",
        "internal": true
      },
      {
        "command": "efficient-todo.stopTask",
        "title": "Stop Task"
      },
      {
        "command": "efficient-todo.removeTask",
        "title": "Remove Task"
      }
    ],
    "menus": {
      "view/item/context": [
        {
          "command": "efficient-todo.startTask",
          "when": "view == taskManager",
          "group": "inline"
        },
        {
          "command": "efficient-todo.stopTask",
          "when": "view == taskManager",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Pomodoro Settings",
      "properties": {
        "pomodoro.duration": {
          "type": "number",
          "default": 25,
          "description": "Duration of a pomodoro session in minutes"
        }
      }
    }
  },
  "scripts": {
    "build": "mkdir -p out && cp -r src/* out && cp -r media out && cp package.json out && cp README.md out",
    "vscode:prepublish": "npm run build",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "vscode": "^1.1.36",
    "node-cron": "^3.0.0"
  }
}
